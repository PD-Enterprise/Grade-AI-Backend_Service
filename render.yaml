services:
  - type: web
    name: Grade-AI-Backend_Service
    env: docker
    buildCommand: docker build -t Grade-AI-Backend_Service .
    startCommand: docker run -p $PORT:$PORT Grade-AI-Backend_Service
    envVars:
      - key: PORT
        value: 10000